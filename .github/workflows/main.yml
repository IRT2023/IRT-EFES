name: CI
on:
  push:
    branches: [ master ]
  #pull_request:
   # branches: [ master ]

  workflow_dispatch:

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest

    steps:
      - name: Run a one-line script
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          password: ${{ secrets.PASSWORD }}
          script: cd /var/www/html/IRT-EFES && git pull && curl http://178.62.67.18/admin/rdf/harvest/all.html && curl http://178.62.67.18/admin/solr/index/all.html
